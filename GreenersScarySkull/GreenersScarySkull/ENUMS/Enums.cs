namespace GreenersScarySkull.ENUMS
{

    public enum LocalSensorType
    {
        ActiveHigh, // Sensor pin is pulled high when motion is detected 
        ActiveLow   // Sensor pin is pulled low when motion is detected 
    }
}
